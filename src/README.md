# Source

This folder contains application code for the monorepo.

## Projects

- [UI](UI/README.md): bare-bones React UI scaffold.

The first UI pass is intentionally domain-neutral. Product-specific workflows should be added only after product orientation is captured in `Docs`.
